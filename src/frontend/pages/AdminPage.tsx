import { Box, Typography } from '@mui/material';

export function AdminPage() {
  return (
    <Box>
      <Typography variant="h4">Admin - Schema Editor</Typography>
      <Typography variant="body1">TODO: Implement schema editor</Typography>
    </Box>
  );
}
